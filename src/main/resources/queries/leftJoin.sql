---Couldn´t try so much of this.

SELECT * FROM accounts LEFT JOIN cards ON idAccounts=1;
SELECT * FROM employees LEFT JOIN phones ON idEmployees=2;
SELECT * FROM Cards LEFT JOIN Accounts ON idCards=2;